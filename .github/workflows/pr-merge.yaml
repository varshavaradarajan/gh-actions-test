name: test-pr-merge
on:
  pull_request:
    types: [closed]
jobs:
  delete_tags:
    name: Delete PR tags from Dockerhub
    runs-on: ubuntu-latest
    steps:
      - name: dockerhub task
        env:
          BRANCH: ${{ github.head_ref }}
        run: |
        BRANCH=$(echo -n ${BRANCH} | tr -c '[:alnum:]._-' '-')
        TOKEN=$(curl -s -H "Content-Type: application/json" -X POST -d '{"username": "'${{ secrets.DockerHubUser }}'", "password": "'${{ secrets.DockerHubToken }}'"}' https://hub.docker.com/v2/users/login/ | jq -r .token)
        curl -s -X DELETE -H "Authorization: JWT ${TOKEN}" https://hub.docker.com/v2/repositories/varshavs/csi-do-plugin/tags/${BRANCH}/
